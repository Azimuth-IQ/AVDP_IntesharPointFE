{
  "name": "inteshar-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright UI E2E for Inteshar Point — drives the Flutter (CanvasKit) web app through its accessibility/semantics tree against the live UAT stack.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
